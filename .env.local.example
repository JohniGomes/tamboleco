NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Chave gratuita do Google AI Studio (https://aistudio.google.com/apikey) para o Chat com IA
GEMINI_API_KEY=your-gemini-api-key

# Segredo para proteger a rota /api/keep-alive (chamada 1x/dia pelo Vercel Cron
# para o projeto Supabase gratuito não pausar por inatividade). Gere com:
# node -e "console.log(require('crypto').randomBytes(24).toString('hex'))"
CRON_SECRET=your-random-secret
